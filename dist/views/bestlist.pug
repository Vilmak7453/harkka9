extends layout

block head
	script(src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js")

block content
	div(id="list")
		h1=title
		ul
			nice-thing(
				v-for="item in things", 
				v-bind:thing="item", 
				v-bind:key="item.id",
				v-on:remove="removeThing(item.id)"
			)
		input(v-model="newthing")
		button(v-on:click="addThing") Lisaa kiva juttu
		p {{ errortext }}
	
	script(type="text/javascript", src="../main.js")